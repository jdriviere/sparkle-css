Things to implement in S3CSS Framework:

FEATURES:
    - S3CSS-Modals;
    - S3CSS-Thumbnails;
    - S3CSS-Inputs;
    - [add more UI];

IMPLEMENTATIONS:
    - JS/jQuery implementation (for CSS code fixes if needed); <--
    - SASS/SCSS implementation (for CSS clean-up); <-- ?
    - GulpJS implementation (for *.min.css | *.min.js); <-- ?

SUGGESTIONS:
    - Find coders for help? <-- ?
    - If using following branches, make sure you only work with the specified branch!
        + master : Combination of all final compilations of scripts;
        + CSSBranch : Combination of all CSS file compilations;
        + JSBranch : Combination of all JS file compilations;
        + HTMLBranch : Combination of all HTML file compilations;
        + AddOnBranch : Keeps track of all changes added into S3CSS framework;
        + of all HTML file compilations;
    - [add more here]

FUTURE IMPLEMENTATIONS (v2.0.0):
    - Specific UI color theme;
        + Dark-red;
        + Dark-grey;
        + Light-grey;
        + White;
    - Implement Light theme and Dark theme <-- ?
